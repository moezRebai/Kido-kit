// Injected by build.js via esbuild's `define` — the package.json version,
// baked in at build time so `kido --version` never needs runtime filesystem
// lookups (unreliable across different global-install layouts).
declare const __KIDO_VERSION__: string;
